{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "intercal",
            "request": "launch",
            "name": "Debug INTERCAL Program",
            "program": "${file}",
            "compiler": "${workspaceFolder}/../cringe/cringe.csproj",
            "syslib": "${workspaceFolder}/syslib64.dll"
        }
    ]
}
